# Assets

| File | Origin | Notes |
|---|---|---|
| `icon-128.png`, `app.ico` | `website/public/icon-128.png` | The product icon, shared with the website so the ecosystem has one face. `app.ico` is generated from it (16→128). |
| `logo.svg` | `website/public/logo.svg` | The product logo, kept for future use. |
| `asymptomatik-full.png` | `asymptomatikgames/logo-full-640.webp` | Publisher signature. **Derived**: the source is black line art on an opaque white background, which would be invisible on a dark interface. Opacity is taken from each pixel's darkness and the ink kept **black**, so the white comes from the band behind it — same white, no seam — and the anti-aliasing of the strokes survives instead of turning into a staircase. Drawn black on a white band rather than inverted to white: the mascot is hand-drawn line art, and inverted strokes read as a negative rather than as a drawing. Regenerate the same way if the source changes. |

⚠ The gear mark (`asymptomatikgames/gear-icon.webp`) is **ASymptOmatik**, not ASymptOmatik Games.
It is deliberately not used here: this product is published by ASymptOmatik Games, and the
full logo is the only one that says so.
